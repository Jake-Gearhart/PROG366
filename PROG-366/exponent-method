GetPow(baseNum, powNum)
    result = 1

    for i = 0 to i = powNum - 1
        result = result * baseNum

    return result


Precondition:
    The inputs baseNum and powNum are positive integers.

Postcondition:
    The function returns baseNum to the power of powNum.

Time:
    The function will complete in O(powNum) time, or O(n).
    When powNum = 1, the function will complete in O(1) time, which is the best-case scenario.

Space:
    The function will complete in O(1) space because there are a fixed number of variables.
